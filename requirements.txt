# PyTorch: pick the build for your platform from https://pytorch.org/get-started/locally/
# (e.g. `pip install torch --index-url https://download.pytorch.org/whl/cu128` for recent NVIDIA GPUs).
torch>=2.2
numpy>=1.24
pillow>=10
scikit-learn>=1.3
pandas>=2
matplotlib>=3.7
